{
  "name": "@l1/adapters-logger",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(adapters-logger) no lints configured\"",
    "test": "vitest run --passWithNoTests",
    "format": "echo \"ok\""
  },
  "dependencies": {
    "@l1/contracts": "workspace:*",
    "@l1/ports": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.6.2",
    "vitest": "1.6.0",
    "@types/node": "20.14.12"
  }
}
