# Implementation review and verification

September 16, 2026. Prepared on `codex/portable-league-database` from canonical main `9325a979e4883fb0ed10a83f2edf4d8eda618d48`. This records implementation review and completed isolated verification. The captured database catalog is reviewed separately in `catalog-review.md`; neither document authorizes a production release.

## Local verification

- Final `pnpm verify` after release-tooling corrections: lint, generated Next route types, TypeScript, 2,675 passing unit/runtime checks across 150 files, one environment-dependent network-interface skip, and production build passed.
- Complete Chromium suite: 82 passed at four workers. Initial ten-worker run passed 81 with one `route.fetch` ECONNRESET during local fixture injection; the unchanged complete rerun passed. No application exception was found in the inspected failed-request trace.
- Real Node administration CLI startup outside Next is covered; missing authority is rejected before database/provider operations. Shadow composition verifies normalization, exact enrollment and immutable scoring-profile compatibility without a job claim or source-history writes.
- Retained sanitized real settings for all three leagues and the earlier League One/Two roster and matchup shapes are exercised. These prove source shape, not current production parity or historical rule applicability.
- Final full isolated integration suite: **289 passed across 19 files, zero failed or skipped**, in 354.23 seconds. The earlier 281-pass/four-failure run was followed by the documented corrections and this successful complete rerun. The user explicitly approved the dedicated test database reset after the initial automatic approval-review rejection; the approved run retained every identity, sentinel, TLS, role and denylist guard.
- Real PostgreSQL 180006 wrapper verification passed nine checks, including full catalog/ledger rollback, actual commit, installed replay refusal, and same-session clients that continue after an error. The executable bundle requires a fresh dedicated idle SQL connection; its session-marker preamble must never be run inside unrelated pending work.
- The actual administration adapter/writer completed 378 synthetic writes. All 15 new tables allocated 1,040,384 bytes after 33 initial documents, the same after 330 unchanged checks, and 1,245,184 bytes after reversions and corrections. `independent-capacity-review.md` separates these physical measurements from hypothetical season scenarios, serialized payload bytes, network usage and compute. No complete-season or large-fleet capacity guarantee is made.

## Independent review findings and corrections

| Finding | Correction and regression evidence |
| --- | --- |
| Catalog capture concatenated PostgreSQL's internal policy-command type ambiguously | Cast `polcmd` explicitly to text. Actual PostgreSQL 180006 catalog capture succeeded without changing installed migrations. |
| A client continuing after a failed repeat installation could emit a previous installation's success sentinel | Reproduced the false success in isolated PostgreSQL. Clear and commit the session-only marker before the migration transaction, set it transactionally only after all postconditions, and require it plus identity/catalog/ledger in the final sentinel. Same-session replay and wrong-target regressions passed. |
| Fresh cache verification could label an older calculation with newer settings | Compare original and verified raw content hashes; retain new evidence but withhold calculation context if they differ. `runtime.test.ts` tests the source mismatch. |
| Cached checks could falsely refresh source freshness | Store nullable `verified_at` separately; network-only accepted checks advance it. Page reader tests cover stale, unknown, future and zero-TTL evidence plus conflict-first behavior. The passing SQL integration regression proves unknown-cache bootstrap has no verification time, unchanged network checks advance it, and later cache or rejected checks do not freshen it. |
| Latest season lookup could select a registered but unapproved year | Select the latest intended enrollment season before joining its registration. The passing SQL integration regression ignores an unapproved higher registration and rejects an intended season with a missing connection. |
| Historical operator could use the current year's source registry | Exact operator season is passed to the existing enrollment read. Existing calendar/parity restrictions remain in force; this does not authorize historical rescoring. |
| Configuration could change between official observation and publication | Add a publication-time lineage trigger using the verification observation or original snapshot observation; lock and check current context. The passing SQL integration regression changes configuration after observation, rejects stale publication, then permits the same immutable snapshot with a fresh matching verification observation. |
| Metadata turns could permanently skip some historical weeks | Exclude metadata turns from the weekly ordinal. Tests inspect actual non-metadata selections for current weeks 1, 2, 3, 7, 15 and 18. Independent arithmetic review covered every current week 1–18 across three leagues with no missing historical period. |
| Failed metadata bodies have no source observation time | Preserve null evidence time, retain partial/rejected observations, and never advance accepted freshness from them. Pure/runtime checks and the SQL integration regression passed for all four metadata families; failed null bodies retain null observation times while prior complete heads and verification times remain unchanged. |
| Draft/bracket source variants and ownership could be misread | Verify draft catalog/detail league, season and resource identity before requests/acceptance. Preserve unknown fields. Normalize documented bracket advancement forms and typed selections; reject conflicting asset ownership and duplicate identities. |
| Prior all-player membership depended on fixed league names | Migration 017 uses immutable exact-season intended membership and all required distinct profiles; existing leases, parity and publication path remain. The passing SQL integration regression covers arbitrary league keys, shared and distinct profiles, missing season/connection rejection, later-season isolation, and preservation of historical membership after fleet retirement. |

Independent agents reviewed schema/source identity, runtime composition, source normalization, page freshness, publication membership, migration tooling and collection rotation. The actual catalog and release-wrapper review is recorded in `catalog-review.md`; its manifest is approved for rendering. Physical measurements and the final published revision have separate verification records.

The SQL regression evidence above comes from `integration/league-administration.integration-case.ts` and the enrolled-publication case in `integration/all-player-statistics.integration-case.ts`, which invokes `integration/enrolled-publication-fixture.ts`. All were included in the 289-pass full isolated run recorded in `validation-status.json`.

## Release gate

The guarded final integration suite, PostgreSQL 180006 catalog capture, actual wrapper rollback/commit/replay checks, physical storage workload and independent manifest review have passed. Final published-head CI and Preview evidence are recorded on PR #220. A successful Preview is database-disabled and cannot substitute for these database gates. Production release authority is still required; migration, deployment, bootstrap and scheduled-operation evidence are separate events and are not claimed here. Follow `production-runbook.md` and revalidate dated capacity/service evidence at release.
